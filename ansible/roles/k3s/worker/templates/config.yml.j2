# Primary connection via HAProxy VIP
server: https://{{ k3s_registration_ip }}:6443

# Authentication
token-file: {{ k3s_token_file }}

# Worker node configuration
{{ k3s_agent_config | to_nice_yaml }}
